package io.github.safari.lwjgl3.maingame;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;


public class GameModelTest {

    /*
    @Test
    public void testGameModelInitialization() {
        GameModel model = new GameModel(1);

        assertEquals(1, model.getTouristcount());
        assertTrue(model.getMoney() > 0);
        assertFalse(model.getEnvironments().isEmpty());
        assertFalse(model.getRoads().isEmpty());
    }

    @Test
    public void testSumHerbivoresAndPredators() {
        GameModel model = new GameModel(1);

        assertEquals(0, model.sumHerbivores());
        assertEquals(0, model.sumPredators());
    }

    @Test
    public void testCanBuyReturnsTrueIfEnoughMoney() {
        GameModel model = new GameModel(1);

        ShopItem cheapItem = new ShopItem("Tree", 100);

        assertTrue(model.CanBuy(cheapItem));
    }

    @Test
    public void testCanBuyReturnsFalseIfNotEnoughMoney() {
        GameModel model = new GameModel(1);

        ShopItem expensiveItem = new ShopItem("Gold Statue", Integer.MAX_VALUE);
        assertFalse(model.CanBuy(expensiveItem));
    }

     */
}
