stages:
  - build
  - test

build_job:
  stage: build
  image: gradle:8.0-jdk17
  script:
    - gradle build
  artifacts:
    paths:
      - build/libs/

test_job:
  stage: test
  image: gradle:8.0-jdk17
  script:
    - gradle test
